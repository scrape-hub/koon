[package]
name = "koon-r"
version = "0.4.0"
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
extendr-api = "0.8"
koon-core = { path = "../../../core", features = ["doh"] }
tokio = { version = "1", features = ["rt-multi-thread"] }
serde_json = "1"
